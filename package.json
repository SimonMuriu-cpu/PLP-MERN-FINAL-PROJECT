{
  "name": "localmart",
  "version": "1.0.0",
  "description": "Localized E-Commerce Platform with Same-Day Delivery",
  "scripts": {
  "dev": "concurrently \"pnpm run server\" \"pnpm run client\"",
  "server": "cd server && pnpm run dev",
  "client": "cd client && pnpm run dev",
  "build": "cd client && pnpm install && pnpm run build",
  "seed": "cd server && pnpm run seed",
  "install-server": "cd server && pnpm install",
  "install-client": "cd client && pnpm install",
  "install-all": "pnpm run install-server && pnpm run install-client"
},
 "devDependencies": {
    "concurrently": "^8.2.0"
  }
}